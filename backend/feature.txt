"http://localhost:3000/api/auth/me
Backend code for the GET /api/auth/me route
profile.tsx <button className={styles.editBtn}>Edit Profile</button>
<button className={styles.createBtn} onClick={() => alert("Open Modal")}>
            + New Project
          </button>

change role of a user 
add user to a project 
delete user to a project 
add workflow/board to a project
add and delete task from khanban board 
drag and drop task between columns
add delete and reorder columns 
Backend "Me" route 🚀
archive project

WorkflowBoard.tsx:-  
 handleCreateTask 
 handleDeleteTask 
 fetchBoardData 
changewiplimit
wiring for create task
create kanban column
delete kanban column
handleUpdateWip in Kanban Column
handle edit task in workflow board
drag and drop 
comments adding and deleting 
comments tagging users
notification 
side bar for each task for a user 

